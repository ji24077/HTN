.venv/
__pycache__/
*.pyc
.env
*.safetensors
*.db
.pytest_cache/
.ruff_cache/
.DS_Store

# Checkpoint weights are large and reproducible; *.safetensors above already
# excludes them. ckpt/*/meta.json is small and IS the measurement evidence,
# so the directory itself is deliberately NOT ignored.
