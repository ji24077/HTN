"""Ethan — T-1. Training loop. Consumes JobConfig verbatim.

EVENTS TO STDOUT, LOGS TO STDERR. See contracts.emit().
"""

import logging
import sys

logging.basicConfig(stream=sys.stderr, level=logging.INFO)


def main() -> None:
    raise SystemExit("T-1 not implemented yet")
