# RTX 4090

Key: `rtx4090`

| | |
|---|---|
| VRAM | 24 GB |
| Measured here | yes |
| Batch-1 median | 0.639s |

Latency is the batch-1 median from the 13-case gate in the hardware
matrix, one model across every card, so the cards compare to each other.

## Holds

- Qwen2.5-0.5B
- Qwen3-4B-Instruct

## Validated optimisations

- `static_kv_cache` — Preallocated KV cache; no per-step reallocation.

## Refused here

- `torch_compile` — median improved 0.130s -> 0.115s but p95 went 0.132s -> 1.507s, worse than eager's 0.404s; measured by scripts/probe_decode_latency.py
- `batch_16` — throughput gate passed but the 300-case output check did not

## Evidence

demo/results/hardware-matrix-2026-09-19/rtx4090/ and scripts/probe_decode_latency.py

The fastest measured card here for one request, and the one torch.compile was tried and refused on.

---

Generated from `src/gpushare/agent/gpus.py`; a test fails if the two
disagree. Edit the table, not this file.
