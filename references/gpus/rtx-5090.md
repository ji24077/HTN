# RTX 5090

Key: `rtx5090`

| | |
|---|---|
| VRAM | 32 GB |
| Measured here | **no** |
| Batch-1 median | — (nothing has run on it) |

Latency is the batch-1 median from the 13-case gate in the hardware
matrix, one model across every card, so the cards compare to each other.

## Holds

- Qwen2.5-0.5B
- Qwen3-4B-Instruct

## Validated optimisations

- none yet

## Refused here

- nothing has been refused on this card

## Evidence

none — the hardware matrix lists it with `measured: false`

Listed so a recommendation can name it. Nothing has run on it, so it has no latency and no validated optimisation. Any figure shown for it is an estimate and must be labelled one.

---

Generated from `src/gpushare/agent/gpus.py`; a test fails if the two
disagree. Edit the table, not this file.
